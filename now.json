{
  "builds": [
    {
      "src": "/**/*.ts",
      "use": "@now/node"
    }
  ],
  "routes": [
    {
      "src": ".*",
      "headers": {
        "Access-Control-Allow-Origin": "http://localhost http://localhost:3000 http://*.now.sh"
      }
    }
  ]
}